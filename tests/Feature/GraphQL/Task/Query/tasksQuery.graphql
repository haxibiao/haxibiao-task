#任务列表查询
query tasks($type: TaskType!) {
  tasks(type: $type) {
    id
    name
    user {
      name
      id
    }
    details
    taskInfo {
      method
      router
    }
    type
    task_status
    progress_details
    task_progress
  }
}
